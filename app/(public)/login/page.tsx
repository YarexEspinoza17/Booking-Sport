export default function LoginPage() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Login</h1>
      <p>Placeholder para autenticación (NextAuth / Supabase Auth)</p>
    </main>
  );
}
